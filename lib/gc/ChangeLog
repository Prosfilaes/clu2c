Thu Feb 06 04:44:02 1997  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Version 1.99.3 (2.0-beta3) released.

Mon Jun 10 09:54:00 1996  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Version 1.99.2 (2.0-beta2) released.

Thu May 16 05:53:41 1996  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* gc_conf.h (ALIGNMENT): Left to 4 for DJGPP Version 2.

Wed May 15 14:22:39 1996  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Makefile.in (gctest.exe, setjmp_t.exe): Invoke coff2exe only if
        DJGPP is defined to 1.

	* gc_conf.h: Distinguish between DJGPP Version 1 and 2.

	* os_dep.c: Typedef caddr_t by myself if DJGPP Version 2 is used.

Sun May 12 07:23:03 1996  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* gc_conf.h, gc_priv.h, if_mach.c, if_not_there.c, mach_dep.c,
        os_dep.c, setjmp_t.c, test.c: Doc fix.

Sat Apr 27 05:09:27 1996  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Makefile.in: Removed `remove.bat' from the list of non-source
        files.

Sun Jan 21 13:41:15 1996  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Makefile.in: Replaced `$(CFLAGS)' with `$(ALL_CFLAGS)' in order
	to ensure that <config.h> can be included.

	* gc_conf.h (DYNAMIC_LOADING): Defined only if the macro
	`DISABLE_DYNAMIC_LOADING' is undefined.  On SunOS 3.x and SunOS
	4.0.x, the user must explicitly specify the option
	`--disable-dynamic-loading' when invoking the `configure' script.

	* gc_priv.h: Do not define `clock_t': now it is defined in
        <config.h> if necessary.

	* gc_conf.h: Do not deal with the macros `SUNOS3' and `SUNOS40'.
	* mach_dep.c: Do not check for the macro `SUNOS3'.

	* gc_conf.h: Include <config.h> at the top build directory.
	* Makefile.in (DEFAULT_CFLAGS): Added `-I$(topdir)' for including
        <config.h>.

	* gc_conf.h: Renamed from `config.h'.
	* Makefile.in, gc_priv.h, if_mach.c, if_not_there.c, setjmp_t.c,
        test.c: Changed accordingly.

Sat Jan 20 14:03:04 1996  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Makefile.in: Added `add_gc_prefix.c', `gcc_support.c', and
        `weakpointer.h' to the list of miscellaneous source files.

	* Makefile.in (mark_rts.o): Changed according to the changes
        between GC 4.5 and GC 4.8.

Fri Jan 19 08:30:20 1996  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* config.h: Arranged djgpp port.
	* os_dep.c: See `DJGPP' instead of `GO32'.

	* BCC_MAKEFILE, NT_MAKEFILE, OS2_MAKEFILE, blacklst.c, finalize.c,
        gc_cpp.h, mach_dep.c, mark.c: Merged the changes between GC 4.4
        and GC 4.8.

	* MacOS.c: Merged the changes between GC 4.3 and GC 4.8.

	* Makefile.gc, README.debugging, README.gc, allchblk.c, alloc.c,
        checksums.c, config.h, dbg_mlc.c, dyn_load.c, gc.h, gc_priv.h,
        malloc.c, mark_rts.c, misc.c, os_dep.c: Merged the changes between
        GC 4.5 and GC 4.8.

	* gc_hdrs.h, obj_map.c: Merged the changes between GC 4.2 and GC
        4.8.

Thu Jan 18 14:24:48 1996  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* ptr_chck.c, test_cpp.cc: Merged the changes between GC 4.4 and
        GC 4.8.

	* setjmp_t.c: Merged the changes between GC 4.3 and GC 4.8.

	* stubborn.c, typd_mlc.c: Merged the changes between GC 4.2 and GC
        4.8.

	* test.c: Merged the changes between GC 4.5 and GC 4.8.

	* add_gc_prefix.c, gcc_support.c, weakpointer.h: New files in GC
        4.8.

Fri Nov  3 12:19:53 1995  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Makefile.in (setjmp_test): Changed according to the changes
        between GC 4.4 and GC 4.5.

	* Makefile.gc, README.gc, README.win32, allchblk.c, alloc.c,
        checksums.c, config.h, gc.h, gc_priv.h, malloc.c, misc.c,
        os_dep.c, reclaim.c, test.c: Merged the changes between GC 4.4 and
        GC 4.5.

	* PCR-Makefile, pcr_interface.c: Merged the changes between GC 4.2
        and GC 4.5.

	* README.debugging, dbg_mlc.c, dyn_load.c, mark_rts.c: Merged the
        changes between GC 4.3 and GC 4.5.

Sat May 20 11:55:03 1995  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Makefile.in: Make use of MK_MISC_SOURCES and MK_NON_SOURCES.

Sat May 13 09:37:09 1995  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Makefile.in: Renamed AOUT2EXE as COFF2EXE.

Sat May  6 12:26:16 1995  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Makefile.in: Added remove.bat to the list of miscellaneous
        source files.

Fri May  5 06:41:00 1995  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* config.h (I386/GO32): Define ALIGNMENT as 2.

Wed May  3 13:00:09 1995  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Makefile.in: Support for MS-DOS and X68000 for making check.

	* config.h (I386/GO32): Define DATASTART using the address of
        environ.

Sun Apr 30 12:12:02 1995  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Makefile.in: Restricted the use of MK_PATH_NAME.

Fri Mar 31 11:59:54 1995  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Makefile.in: Changed the definition of ARCHIVE.
	(install): Do nothing.

	* Makefile.in: Define MISC_SRC.

Thu Mar 30 10:54:34 1995  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Makefile.in (install): Do ranlib after installation.

Wed Mar 29 13:46:09 1995  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* Makefile.in: New file.

Sat Mar 25 13:13:55 1995  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* BCC_MAKEFILE: New file in GC 4.4.

	* MacProjects.sit.hqx, Makefile.gc, NT_MAKEFILE, OS2_MAKEFILE,
	README.gc, README.win32, alloc.c, config.h, finalize.c, gc.h,
	gc_cpp.h, gc_priv.h, mach_dep.c, malloc.c, mark.c, misc.c,
	os_dep.c, ptr_chck.c, test.c, test_cpp.cc: Merged the changes
	between GC 4.3 and GC 4.4.

	* README.Mac, README.amiga, SCoptions.amiga, SMakefile.amiga,
        allchblk.c, blacklst.c, checksums.c, reclaim.c: Merged the changes
        between GC 4.2 and GC 4.4.

	* os_dep.c (clock): Removed.

	* gc_priv.h: Do not typedef ptrdiff_t on NEWS-OS 4.2R: it is
	defined in <stddef.h>.  Define BSD_TIME on NEWS-OS 4.2C.  Removed
	workarounds about clock and clock_t on NEWS-OS 4.x.

	* EMX_MAKEFILE, MacOS.c, MacProjects.sit.hqx, Makefile.gc,
        NT_MAKEFILE, OS2_MAKEFILE, README.gc, README.win32, alloc.c,
        config.h, dbg_mlc.c, dyn_load.c, finalize.c, gc.h, gc_mark.h,
        gc_priv.h, headers.c, if_mach.c, mach_dep.c, malloc.c, mark.c,
        mark_rts.c, misc.c, os_dep.c, pc_excludes, setjmp_t.c,
        sol_threads.c, test.c: Merged the changes between GC 4.2 and GC
        4.3.

	* alpha_mach_dep.s: New file in GC 4.2.

	* Makefile.gc, README.gc, allchblk.c, alloc.c, gc.h, mach_dep.c,
        mark_rts.c, mips_mach_dep.s, misc.c, reclaim.c, setjmp_t.c,
        test.c: Merged the changes between GC 1.6 and GC 4.2.

	* os_dep.c (GO32): Do not use signal facilities.  Do not declare
        sbrk.
	(handler): Alternative definition for NEWSOS4.
	(clock): New function for NEWSOS4.

	* gc_priv.h (GO32): Define DISABLE_SIGNALS and ENABLE_SIGNALS as
        void.
	(NEWSOS4): Declare clock.
	(SUNOS3, SUNOS40, NEWSOS4): Typedef clock_t.

	* config.h: Define NEWSOS4 for NEWS-OS Release 4.  Define SUNOS40
	for SunOS 4.0.x.  Do not define SUNOS4 if SUNOS3 is defined.
	(I386/GO32): Define OS_TYPE, DATASTART, and STACKBOTTOM.
	(M68K/NEWSOS4): Define OS_TYPE, DATASTART, and HEURISTIC2.  Declare
	etext.
	(M68K/SUNOS3): Define OS_TYPE.  Do not define DYNAMIC_LOADING.
	(MIPS/NEWSOS4): Define OS_TYPE.  Cancel HEURISTIC2 and explicitly
	define STACKBOTTOM for pixie to work.
	(SPARC/SUNOS40): Define OS_TYPE.  Do not define DYNAMIC_LOADING.

	* EMX_MAKEFILE, MacOS.c, MacProjects.sit.hqx, NT_MAKEFILE,
        OS2_MAKEFILE, PCR-Makefile, README.Mac, README.OS2, README.QUICK,
        README.amiga, README.win32, SCoptions.amiga, SMakefile.amiga,
        barrett_diagram, blacklst.c, callprocs, checksums.c, config.h,
        dbg_mlc.c, dyn_load.c, finalize.c, gc.man, gc_hdrs.h, gc_mark.h,
        gc_priv.h, gc_private.h, gc_typed.h, headers.c, if_mach.c,
        if_not_there.c, malloc.c, mark.c, new_hblk.c, obj_map.c, os_dep.c,
        pc_excludes, pcr_interface.c, real_malloc.c, rs6000_mach_dep.s,
        sol_threads.c, sparc_mach_dep.s, stubborn.c, typd_mlc.c: New files
        in GC 4.2.

Sat Oct 30 08:01:38 1993  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* test.c, misc.c, gc.h, alloc.c: Support for the new GC library's
        naming convention.

Thu Nov 19 02:58:42 1992  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* mach_dep.c: Changed for gcc + gas on CISC NEWS.

Wed Nov  4 06:57:30 1992  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* gc.h (FLOAT_HZ, DATASTART): Support for CISC NEWS.

Wed Oct 14 08:44:34 1992  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* alloc.c (gc_now, gc_cnt): New global variables.

Wed Sep 23 09:23:25 1992  Hirofumi TOUHEI  <toh-hei@is.titech.ac.jp>

	* Version 1.1 released.

Mon Jul 13 12:02:10 1992  8928693 morimura takeshi  <m-takesi@is.titech.ac.jp>

	* allchblk.c (allochblk): Set uninitilization flag before freeing
	heap block.
	* alloc.c (gcollect, expand_hp): Likewise.

Mon Mar 23 20:01:19 1992  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* gc.h: Fixed bug about ALIGNMENT and DATASTART.

	* gc.h: Support for RISC NEWS, CISC NEWS, MIPS RC6280.

	* mach_dep.c: Support for CISC NEWS.

	* misc.c: Doc fix. 

Mon Jul  8 20:10:04 1991  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* misc.c (gc_malloc): Return NULL if lb == 0.

Fri Jul  5 17:43:24 1991  Tetsu Ushijima  <ushijima@is.titech.ac.jp>

	* gc.h: Support for RISC NEWS.

	* README.gc, allchblk.c, alloc.c, gc.h, mach_dep.c, mark_rts.c,
        mips_mach_dep.s, misc.c, reclaim.c, setjmp_t.c, test.c: New files.
